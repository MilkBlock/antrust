let _'defunc_apply =
  let rec _'defunc_apply =
    fun _'f _'a ->
      match (_'f, _'a) with
      | `C_'lam0, xs, ys ->
        match xs with
        | Nil -> ys
        | Cons (xh, xt) ->
          (Cons (xh, (_'defunc_apply global:append xt ys)))
  in
  _'defunc_apply;;
type int_list = | Nil | Cons of int * int_list;;
let () =
  let rec append = `C_'lam0 in ();;
let _ =
  _'defunc_apply global:append (Cons (1, Nil)) (Cons (2, (Cons (3, Nil))));;