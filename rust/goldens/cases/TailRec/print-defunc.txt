let _'defunc_apply =
  let rec _'defunc_apply =
    fun _'f _'a ->
      match (_'f, _'a) with
      | `C_'lam0, x, s ->
        match x with
        | Nil -> s
        | Cons (xh, xt) -> (_'defunc_apply global:sum xt (xh + s))
  in
  _'defunc_apply;;
type int_list = | Nil | Cons of int * int_list;;
let () =
  let rec sum = `C_'lam0 in ();;